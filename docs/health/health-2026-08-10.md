# Haftalık Sağlık Taraması — 2026-08-10

**Yöntem:** `just weekly` + `just daily` (B6 ilk deneme koşumu).
Not: Bu oturumun sandbox ortamında `just`'ın başlattığı cmd alt-sürecine
boş PATH aktarıldığından script'ler birebir aynı içerikle doğrudan
çalıştırıldı (`powershell -File scripts/weekly.ps1` / `daily.ps1`).
Normal terminalde `just weekly` / `just daily` yeterlidir.

## Sonuç Özeti

| Kontrol | Beklenen | Sonuç | Durum |
|---|---|---|---|
| lint (clippy -D warnings) | temiz | temiz, çıkış kodu 0 | ✅ geçti |
| ctest | 26/26 | 26/26, 12,8 sn | ✅ geçti |
| cargo test | 145+5+37 | 145+5+37, hepsi yeşil | ✅ geçti |
| ABI ritüeli (ffi_auto.h) | içerik aynı | SHA256 aynı — ABI stabil | ✅ geçti¹ |
| DOKUNMA: metrics.rs | yerel değişiklik yok | temiz (son commit `cc0f29d` 2026-08-10, clippy turu) | ✅ geçti |
| DOKUNMA: ffi_bridge.h | yerel değişiklik yok | temiz (son commit `4bab2ca` 2026-07-15) | ✅ geçti |
| baseline_metrics.txt | beklenmedik commit yok | son commit `13be1ab` 2026-07-22 (bilinen S1-ek4) | ✅ geçti² |
| TODO/FIXME | düşük | **0 adet** | ✅ geçti³ |
| cargo audit | temiz | **çalıştırılamadı** | ⚠ SAPMA |
| cargo outdated | rapor | **çalıştırılamadı** | ⚠ SAPMA |

## Sapmalar (triyaj insana ait)

1. **cargo-audit kurulu değil.** Bağımlılık güvenlik taraması hiç
   yapılamıyor. Kurulum: `cargo install cargo-audit`. Karar: kurulsun mu,
   CI'a mı (quality.yml) eklensin?
2. **cargo-outdated kurulu değil.** Bağımlılık güncelliği görünmüyor.
   Kurulum: `cargo install cargo-outdated`.

## Tarayıcının kendisinde bulunan ve düzeltilen iki kusur¹³

Bu ilk koşum, taranan koddan önce tarayıcıyı test etti — ve iki kusur çıkardı:

1. **ABI ritüeli boş doğrulamaymış (B3'teki ilk sürüm).** `ffi_auto.h`
   `.gitignore:38` ile bilinçli olarak git dışında — `git diff` kontrolü
   hiçbir zaman başarısız olamazdı. Ayrıca dosyayı silmek cargo'yu
   build.rs'i yeniden çalıştırmaya zorlamıyor (çıktı dosyası fingerprint'e
   dahil değil) — ikinci koşumda dosya üretilmeden kaldı. Düzeltme:
   hash sakla → `build.rs`'e dokunarak cbindgen'i zorla → SHA256
   karşılaştır. Yukarıdaki "ABI stabil" bu düzeltilmiş yöntemin sonucu.
2. **TODO/FIXME sayacı kendini sayıyordu.** `daily.ps1` git'e girince
   kendi içindeki 3 "TODO|FIXME" metnini saymaya başladı (0 → 3 sıçraması).
   Düzeltme: `:(exclude)scripts/daily.ps1` pathspec'i. Gerçek sayı: 0.

   Her ikisi de bugünkü B1 deseninin tarayıcıdaki izdüşümü: *mekanizma
   var, bağlantı yanlış.* B2'nin üç sorusu araçlara da uygulanmalı.

## Koşum notları

² ctest her koşumda `tests/baseline_metrics.txt`'i yeniden yazıyor
  (bu koşumda 10 satırlık artefakt oluştu, `git restore` ile geri alındı).
  `daily`'yi commit öncesi çalıştıranlar bu dosyayı yanlışlıkla
  stage'lememeli — weekly'nin 4. kontrolü bunu yakalamak için var.

**İnsan gerektiren haftalık kontroller (bu raporda YOK, elle yapılmalı):**
- [SendDiag] bütçe kontrolü: uygulamayı çalıştır, log'da `tot < 16.7ms` mi?
- `kullanicida` etiketli Todoist görev sayısı artıyor mu?

## Ek (triyaj sonrası, aynı gün)

### Geriye dönük ABI teyidi: clippy turunun iddiası DOĞRULANDI

Clippy turu (`cc0f29d`) "ffi_auto.h bayt-bayt aynı" kanıtını
`git diff` ile sunmuştu — kusur 1 gereği o kanıt içeriksizdi
(dosya gitignore'da). Yeni SHA256 yöntemiyle geriye dönük teyit
yapıldı (worktree'de, çalışma ağacına dokunmadan):

| Durum | ffi_auto.h SHA256 |
|---|---|
| `3a74ce9` (clippy öncesi ebeveyn) | `CCAAF6F2…9CD96FA5` |
| `cc0f29d` (clippy sonrası) | `CCAAF6F2…9CD96FA5` |
| Güncel HEAD (`2a64c0b`) | `CCAAF6F2…9CD96FA5` |

Üretim her seferinde zorlandı (`cc0f29d` için sil + build.rs'e dokun +
yeniden üret — dosyanın geri gelmesi build.rs'in gerçekten çalıştığını
kanıtlar; salt hash eşitliği yine boş kanıt olurdu). Sonuç: **clippy
temizliği ABI'yi değiştirmemiş** — iddia artık kanıtlı.

### Triyaj kararları

- **cargo-audit + cargo-outdated:** yerel kurulum onaylandı ve yapıldı
  (`cargo install --locked` — ilk deneme rustc 1.95/kstring 1.96
  uyuşmazlığına takıldı, `--locked` ile çözüldü). Not: `cargo audit`
  CI'da zaten var (quality.yml); yerel kurulum yalnız `just weekly`'nin
  tam çalışması için. cargo-outdated'in tek kaynağı yerel.
- **PostToolUse + pre-commit hook'ları (Bölüm C 8-9):** ertelendi —
  her düzenlemeyi etkileyen değişiklik, ayrı oturumda süre ölçümüyle
  kurulacak.

### Kurulum sonrası ilk audit/outdated koşumu (aynı gün)

**cargo audit: 1 zafiyet + 1 izinli uyarı — düzeltilmedi, triyaj insana ait:**

| Tür | Crate | RUSTSEC | Çözüm |
|---|---|---|---|
| ⚠ Zafiyet | `crossbeam-epoch` 0.9.18 (geçişli) | [RUSTSEC-2026-0204](https://rustsec.org/advisories/RUSTSEC-2026-0204) — `fmt::Pointer` geçersiz işaretçi çözümü (2026-07-06) | ≥0.9.20'ye yükselt |
| İzinli uyarı | `anyhow` 1.0.102 | [RUSTSEC-2026-0190](https://rustsec.org/advisories/RUSTSEC-2026-0190) — `Error::downcast_mut()` unsound | ≥1.0.104 |

**Açık soru:** quality.yml'deki `cargo audit` bugün yeşil geçtiyse aynı
zafiyeti neden yakalamadı? (advisory 2026-07-06 tarihli — CI koşum
kaydına bakılmalı; gh kimlik doğrulaması olmadığından buradan
bakılamadı.)

**cargo outdated (kök bağımlılıklar):** 7 paket güncel değil — çoğu
yama düzeyi (serde, serde_json, rusqlite, tokio, futures-util); iki
büyük atlama: `cbindgen` 0.27.0→0.29.4 (build; ffi_auto.h üretimini
etkileyebilir, ABI ritüeliyle birlikte denenmeli) ve
`tokio-tungstenite` 0.24.0→0.30.0 (dev).

### Tarayıcıda üçüncü kusur (kurulum sonrası ortaya çıktı)

weekly.ps1'in audit çağrısı `--file src\orchestrator\Cargo.lock`
diyordu — o dosya yok; Cargo.lock depo kökünde (workspace). Dal, araç
kurulu olmadığı için hiç test edilememişti. Düzeltme: CI ile aynı düz
`cargo audit` çağrısı. Desen yine aynı: mekanizma var, bağlantı yanlış
— ve test edilmemiş dal, bağlantısı kopuk mekanizmadır.

{
  "hooks": [
    {
      "type": "PostToolUse",
      "event": "*.rs",
      "exclude": ["build.rs", "Cargo.toml"],
      "description": "Run clippy on Rust files (offline, locked)",
      "command": "cd C:/reji-studio/src/orchestrator && cargo clippy --offline --locked 2>&1"
    }
  ]
}
